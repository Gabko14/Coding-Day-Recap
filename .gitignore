# bv (beads viewer) local config and caches
.bv/

# Claude Code local settings
.claude/

# Compiled Swift binary (cached by calendar_events.py)
scripts/calendar_eventkit

# Override global gitignore — we want beads tracked in this repo
!.beads/
.beads/.bv.lock
